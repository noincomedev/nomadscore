type User {
  _id: ID
  admin: Boolean
  profile: Profile
}

type Profile {
  name: String
  imgurl: String
}

type Query {
  user: User
  nearVenues(near: String): [Venue]
}

input VoteInput {
  venueid: ID
  a: Int
  b: Int
}

type Mutation {
  submitVote(vote: VoteInput!): Venue
}
