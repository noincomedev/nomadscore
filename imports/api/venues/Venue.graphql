type Coords {
  address: String
  lat: Float
  lng: Float
}

type Score {
  a: Float
  b: Float
}

type Venue {
  _id: ID
  providerid: ID
  name: String
  location: Coords
  photourl: String
  score: Score
  voted: Boolean
  type: String
}

extend type Query {
  venue(_id: ID!): Venue
}
